name: myapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^6.1.1        # 既存ならそのまま
  google_sign_in: ^6.2.1        # これを追加

  cupertino_icons: ^1.0.8
  go_router: ^16.2.4
  provider: ^6.1.2
  google_fonts: ^6.3.2
  image_picker: ^1.2.0
  firebase_core: ^4.1.1 # Upgraded version
  firebase_ai: ^3.3.0
  flutter_riverpod: ^3.0.3
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

flutter:
  uses-material-design: true
