# AIルームスキャンアプリ Blueprint

## 1. プロジェクト概要

**コンセプト: 面倒な片付けを、楽しい宝探しに変える。**

このアプリは、ユーザーが部屋の写真を撮るだけで、AIが画像内のアイテムを自動で識別・査定し、フリマアプリへの出品を簡単にするためのものです。「出品は面倒」と感じているユーザーの感情を「面白いかも」という好奇心へと転換させ、発見の喜びを通じてスムーズな出品体験を提供し、隠れた資産価値を最大限に引き出します。

## 2. ターゲットユーザー

*   **ペルソナ**: 20歳以上の男女。「先延ばしにしがちな整理整頓の初心者」。
*   **インサイト**: 不要品を売りたい気持ちはあるが、出品作業の心理的・物理的ハードルが高く、行動に移せていない。

## 3. UI/UXデザイン原則

*   **直感性**: ユーザーに「考える隙」を与えず、次に何をすべきかが一目でわかるUIを徹底します。
*   **フィードバック**: ユーザーのアクションに対し、アニメーションや状態変化で即座に反応を返すことで、操作している感覚と安心感を与えます。
*   **心地よさ**: スムーズなアニメーションや、ポジティブな言葉選びで、「宝探し」のワクワクする体験を演出します。
*   **視覚的ヒエラルキー**: 「次に見てほしい情報」「次にしてほしい行動」が自然と目に入るように、サイズ、色、配置を戦略的に設計します。
*   **クリーンなデザイン**: 十分な余白、一貫性のあるフォント階層、限定されたカラーパレット（ベース、メイン、アクセントの3色）により、洗練された印象と高い可読性を保ちます。

## 4. アプリ体験フローと機能要件

1.  **起動**: 画面中央に巨大なカメラアイコンボタンを一つだけ表示。「何をすべきか」を一瞬で理解させ、行動を促す。
    *   **UI**: 巨大なカメラボタンと、心躍る短いキャッチコピー（例: 「部屋に眠る、お宝を探そう。」）。
2.  **撮影**: ユーザーが部屋の写真を撮る。
    *   **機能**: `image_picker` を利用してカメラまたはギャラリーから画像を取得。
3.  **分析**: AIによる分析が始まる。
    *   **UI**: 「お宝を分析中…」といったメッセージと、ユーザーを待たせるストレスを軽減する楽しいアニメーションを表示。
4.  **発見**: 分析完了後、写真の上にAR風の値札がアイテムごとに表示される。部屋が「宝の山」に変わる、最もワクワクする瞬間。
    *   **UI**: 撮影した画像の上に、AIが検出した各アイテムの推定価格範囲を示す「値札」をオーバーレイ表示。特に高額なアイテムは少し目立たせる演出も検討。
    *   画面上部には「〇個のアイテムを発見！推定総額〇〇円」というサマリーを表示。
5.  **注目**: ユーザーが気になる値札をタップする。
    *   **UI**: タップされた値札がハイライトされ、他の値札は少し暗くなる。同時に、画面下部に**「詳細を見る／出品する」**という明確なCTAボタンが出現する。
6.  **詳細確認と出品意思決定**: ユーザーが「詳細を見る／出品する」ボタンを押す。
    *   **UI**: 画面下からアイテム詳細カード（商品名、ブランド、価格比較など）がスライドアップ表示される。カード内には「出品する」ボタンを配置。
7.  **状態選択**: 「出品する」ボタンを押すと、「商品の状態は？」というシンプルな選択画面に遷移する。
    *   **UI**: 全画面に「新品・未使用」「目立った傷や汚れなし」などの選択肢を大きく表示し、ユーザーがタップで直感的に選べるようにする。
8.  **最終準備とフリマ連携**: 状態を選択すると、説明文が自動生成され、クリップボードにコピーされる。
    *   **UI**: 「最適な説明文を作成中です…」のアニメーション後、「コピーしました！」という通知（トーストやスナックバー）を表示。**この画面で待機し、完了画面には遷移しない。**
    *   同じ写真内の別アイテムを出品したい場合に備え、詳細画面に戻るための「×」ボタンなどを配置。

## 5. エラーハンドリング

*   **コピー失敗対策**: AIが生成する説明文は、フリマアプリ側でエラーが起きにくいシンプルなテキスト形式（特殊記号や過度な改行を避ける）とする。
*   **フィードバック導線**: 説明文生成画面に、常に「うまくいかなかったら…」のようなリンクを配置し、ユーザーがいつでも問題を報告できるフォームへ誘導する。

## 6. 技術仕様と開発計画

1.  **プロジェクトセットアップ**:
    *   `pubspec.yaml`に以下のパッケージを追加: `flutter_riverpod` (or `provider`), `go_router`, `google_fonts`, `image_picker`, `firebase_core`, `firebase_ai`.
2.  **基盤構築**:
    *   `main.dart`でテーマ（カラー、フォント）と`MaterialApp.router`を設定。
    *   `go_router`を使用し、上記の体験フローに沿った画面ルート (`/`, `/scan`, `/result`, `/item/:id`, `/condition`, `/prepare`) を定義。
3.  **画面実装（UI Skeleton）**:
    *   `home_screen.dart`（起動画面）から順に、各画面のUIを静的に構築。まずはダミーデータで見栄えを整える。
    *   AR風値札の表示ロジック（`Stack`ウィジェットと`Positioned`ウィジェット）を実装。
    *   画面下からのスライドアップパネル（`showModalBottomSheet`）や、CTAボタンの表示・非表示ロジックを実装。
4.  **機能連携**:
    *   `image_picker`を実装し、画像選択フローを有効化。
    *   `firebase_ai`連携を実装（まずはモックで）。分析中の待機処理、結果の受け渡し、値札へのデータ反映を行う。
    *   状態選択から説明文生成、クリップボードへのコピー (`Clipboard.setData`) までの一連の流れを実装。
5.  **インタラクションと仕上げ**:
    *   値札の表示アニメーション、ボタンの表示アニメーションなどのマイクロインタラクションを追加。
    *   「コピーしました！」の通知機能を実装。
    *   エラー時のフィードバックフォームへの導線を設置。
